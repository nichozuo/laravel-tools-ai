<?php

namespace App\Models;

{{ importClasses }}

/**
{{ properties }}
 */
class {{ className }} extends Base
{
    {{ useTraits }}

    {{ tableName }}
    {{ comment }}
    {{ fillable }}

    {{ hidden }}
    {{ casts }}
    {{ relations }}
}
